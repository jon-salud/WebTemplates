---
/**
 * ProductShowcase - SaaS & Startups
 * Visual product demo/screenshot section
 */
interface Props {
    industry: {
        name: string;
        colors: {
            primary: string;
            accent: string;
        };
    };
}

const { industry } = Astro.props;

const features = [
    { icon: "⚡", label: "Lightning Fast" },
    { icon: "🔒", label: "Enterprise Security" },
    { icon: "🔄", label: "Real-time Sync" },
];
---

<section
    class="py-20 bg-gradient-to-b from-neutral-900 to-neutral-950"
    aria-label="Product Showcase"
>
    <div class="container mx-auto px-6">
        <div class="text-center mb-12">
            <p class="text-xs uppercase tracking-[0.3em] text-neutral-500 mb-3">
                See It In Action
            </p>
            <h2 class="text-3xl md:text-4xl font-semibold text-white mb-4">
                Powerful, Yet Intuitive
            </h2>
            <p class="text-neutral-400 max-w-2xl mx-auto">
                A clean interface that gets out of your way. Everything you
                need, nothing you don't.
            </p>
        </div>

        <!-- Product Screenshot Mock -->
        <div class="relative max-w-5xl mx-auto">
            <div
                class="rounded-xl overflow-hidden shadow-2xl border border-neutral-800"
            >
                <img
                    src="https://images.unsplash.com/photo-1460925895917-afdab827c52f?auto=format&fit=crop&w=1200&q=80"
                    alt="Dashboard analytics on laptop"
                    loading="lazy"
                    class="w-full h-auto object-cover aspect-video"
                />
            </div>

            <!-- Floating feature badges -->
            <div
                class="absolute -bottom-6 left-1/2 -translate-x-1/2 flex gap-4"
            >
                {
                    features.map((feature) => (
                        <div class="bg-white rounded-full px-4 py-2 shadow-xl flex items-center gap-2">
                            <span>{feature.icon}</span>
                            <span class="text-sm font-medium text-neutral-900">
                                {feature.label}
                            </span>
                        </div>
                    ))
                }
            </div>
        </div>

        <div class="mt-16 text-center">
            <a
                href="#demo"
                class="inline-flex items-center gap-2 px-6 py-3 bg-white text-neutral-900 rounded-full font-medium hover:bg-neutral-100 transition-colors"
            >
                Watch Demo
                <span>▶</span>
            </a>
        </div>
    </div>
</section>
